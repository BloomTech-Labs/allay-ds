# Unit tests

## Unit tests for API

To run:
- Enter virtual environment
- change to `allay-ds-api` directory
- `coverage run --source=. -m pytest ../tests/`

To view:
- Enter virtual environment
- change to `allay-ds-api` directory
- `coverage report`
